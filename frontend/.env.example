# Frontend env — copy to ".env". Vite only exposes vars prefixed with VITE_.
# In Netlify: set these under Site Settings > Environment Variables
VITE_API_URL=http://localhost:5000/api
# GA4 measurement ID (e.g. G-XXXXXXXXXX). Leave blank to disable analytics.
VITE_GA_ID=
