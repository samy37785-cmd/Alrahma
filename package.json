{
  "name": "al-rahma-academy",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,green \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend",
    "setup": "npm install --prefix frontend && npm install --prefix backend",
    "start": "npm run dev",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:backend": "npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "test:coverage": "npm run test:coverage --prefix frontend",
    "e2e": "npm run build --prefix frontend && playwright test",
    "e2e:quick": "playwright test",
    "e2e:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "concurrently": "^10.0.3"
  }
}
