# Copy to lib/db/.env (git-ignored) and fill in the real value.
# Supabase → Project Settings → Database → Connection string → URI
# (use the "Session pooler" or direct-connection string — either works for
# `drizzle-kit push`; avoid the "Transaction pooler" one for migrations).
DATABASE_URL=
