# Netlify deployment config for the Al-Rahma Academy frontend (Vite SPA).
[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

# Every route falls back to index.html so React Router can handle it.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
