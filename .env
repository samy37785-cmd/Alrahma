# Relative path so API calls go through the Vite dev/preview proxy.
# This makes the app work from any device (e.g. your phone on the same Wi-Fi).
# For a deployed site, set this to your public backend URL, e.g. https://api.yourdomain.com/api
VITE_API_URL=/api
