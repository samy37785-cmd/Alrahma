# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

**Al-Rahma Academy** — full-stack Islamic education platform. React 18 SPA + Express 4 API + MongoDB Atlas. Deployed as a monorepo: Vercel serves the pre-built frontend SPA only; the backend runs as a standalone, long-running Express process on Render (`backend/`, started via `node server.js`). `vercel.json` rewrites every `/api/*` request to the Render URL — there is no `api/index.js` serverless function in this repo.

## Commands

All commands run from the repo root unless noted.

```bash
# Development (concurrent frontend + backend)
npm run dev

# Install all dependencies
npm run setup

# Build (frontend only, Vite)
npm run build

# Tests
npm test                     # both suites
npm run test:backend         # Node built-in test runner (backend/tests/)
npm run test:frontend        # Vitest (frontend/src/)
npm run test:coverage        # Vitest + v8 coverage report

# Run a single backend test file
cd backend && node --test tests/coupon.test.js

# Run a single frontend test file
cd frontend && npx vitest run src/path/to/test.spec.js

# Linting & formatting (frontend)
cd frontend
npm run lint                 # ESLint
npm run lint:fix             # ESLint auto-fix
npm run format               # Prettier
```

Frontend dev server: `http://localhost:5173`  
Backend API: `http://localhost:5000`  
Vite proxies `/api/*` → `http://localhost:5000` in both dev and preview.

## Architecture

### Backend (`backend/`)

Entry: `app.js` (Express middleware pipeline) → `server.js` (runner, used both in dev and as the Render start command — there is no separate serverless wrapper).

**Middleware pipeline order** (defined in `app.js`):
1. `helmet()` → `cors()` → `express.raw()` (Stripe webhook) → `correlationId`
2. `express.json()` (100 KB limit) → `cookieParser()` → `sanitizeMongo()`
3. `issueCsrfToken` → `verifyCsrfToken` (double-submit cookie pattern)
4. `requestLogger` (Winston with correlation IDs) → DB connection check
5. Route handlers → `notFound` → `errorHandler`

**Observability:** `/health` is a liveness probe (always fast, no DB check); `/ready` is a readiness probe that additionally checks `mongoose.connection.readyState`, so it accurately reports 503 if MongoDB drops the connection at runtime, not just before the first successful connect. Winston logs to Console always (Render has no persistent disk configured — see `render.yaml` — so file-only logging in production would be unrecoverable after a restart) plus rotating files when `NODE_ENV=production`; `errorHandler` and `requestLogger` both include `req.requestId` so a request's error log line and its completion log line can be correlated. `config/db.js` logs `mongoose.connection` `disconnected`/`reconnected`/`error` events at runtime, not just the initial connect.

**Patterns:**
- All async route handlers must be wrapped in `asyncHandler()` (from `utils/asyncHandler.js`)
- Input validation uses `express-validator` arrays passed inline to routes
- Error responses always use shape `{ message: "..." }`
- Authentication: httpOnly JWT cookies + refresh token family rotation (`models/RefreshToken.js`)
- Admin sessions require TOTP MFA (`speakeasy`) + IP whitelist check on every request
- CSRF: frontend reads `csrf_token` cookie and sends it as `X-CSRF-Token` header on mutations

**Admin API (`/api/v1/admin/*`):** the only privileged-mutation surface in the app. Every admin mutation route — user role/subscription/teacher/family management, staff account creation, manual-payment review, course/enrollment CRUD, blog post CRUD, coupon CRUD, contact-message status updates, certificate issue/revoke, and referral conversion — lives under `routes/v1/admin/` (`usersRoutes.js`, `paymentsRoutes.js`, `coursesRoutes.js`, `enrollmentsRoutes.js`, `blogRoutes.js`, `couponsRoutes.js`, `contactRoutes.js`, `certificatesRoutes.js`, `referralsRoutes.js`, `reviewsRoutes.js`), gated by `verifyAccessToken` (TOTP-MFA `AdminUser` session, `admin_at`/`admin_rt` cookies) + `requirePermissions()` RBAC + `auditFromReq()` audit logging. The corresponding legacy top-level route files (`routes/authRoutes.js`, `paymentRoutes.js`, `courseRoutes.js`, `enrollmentRoutes.js`, `blogRoutes.js`, `couponRoutes.js`, `contactRoutes.js`, `certificateRoutes.js`, `referralRoutes.js`, `reviewRoutes.js`) now expose **only** public/student-facing routes and admin **reads** (e.g. `GET /api/coupons`, `GET /api/contact`, `GET /api/certificates` are intentionally still `protect+adminOnly` — only mutations were migrated); no route in this app authenticates a privileged *mutation* via the regular `User`/`protect`/`adminOnly` path anymore. `financialGuard` (`middleware/maintenanceGuard.js`) is wired at the route level on `PATCH /api/v1/admin/payments/manual/:id` only (not router-wide), blocking approve/reject while `financials_frozen` is set, with a `super-admin` emergency override. RBAC permissions for the content-adjacent resources (`blog:write`, `coupons:write`, `contact:write`, `certificates:write`, `referrals:write`, `reviews:write` in `models/AdminUser.js`) are granted to `admin`/`super-admin` only, mirroring the legacy stack's binary "any admin" access — no new editor/viewer fine-graining was introduced for them. New `AdminUser` accounts (there is no self-registration endpoint) are provisioned via `backend/scripts/createAdminUser.js` (`npm run create-admin -- --name ... --email ... --password ... --role ...`); first login walks through TOTP setup automatically.

**Rate limiting:** Redis-backed in production (set `REDIS_URL`), in-memory fallback in dev. Configured in `config/rateLimit.js` and `config/adminRateLimits.js`. Every store's `rateLimit()` call sets `passOnStoreError: true` — a Redis outage fails *open* (the request proceeds, unlimited, for that one call) rather than 500ing the whole API. `/api/v1/admin/*` traffic is rate-limited twice, from two independent counters: the global `apiLimiter` (mounted on all of `/api` in `app.js`) and the admin-specific `adminApiLimiter` (mounted inside `routes/v1/admin/index.js`) — the effective cap is whichever of the two is hit first.

### Frontend (`frontend/src/`)

Entry: `main.jsx` → `App.jsx` (routes + context providers).

**Context provider stack** (outer to inner):
`ErrorBoundary` → `QueryProvider` (React Query v5) → `ThemeProvider` → `LangProvider` → `AuthProvider` → `AdminAuthProvider` → `BrowserRouter`

**Error reporting:** Sentry (`@sentry/react`) is initialized in `main.jsx` (`utils/sentry.js`) whenever `VITE_SENTRY_DSN` is set, and catches uncaught errors/rejections automatically — but `ErrorBoundary.jsx`'s `componentDidCatch` also explicitly calls `captureException`, because a React error boundary catching a render error is exactly what prevents that error from ever reaching Sentry's automatic global handlers.

**State management:**
- Server state: React Query v5 (`@tanstack/react-query`) — all API calls go through custom hooks in `hooks/`
- Client state: React Context (auth, theme, language)
- Forms: React Hook Form v7

**API layer** (`api/`):
- `client.js` — Axios instance barrel (re-exports from domain files)
- `http.js` — low-level HTTP utility that attaches the CSRF token header
- 15+ domain files: `authApi.js`, `courseApi.js`, `paymentApi.js`, etc.
- `quran.js` — calls the external Quran.com API (not the backend)
- `adminHttp.js` — separate axios instance for `/api/v1/admin/*` calls (`adminApi.js`, the admin parts of `paymentApi.js`); on a 401 it clears the cached admin profile and hard-redirects to `/admin/login`, since a stale `admin_at` cookie (15 min lifetime) means a different thing than a regular-session 401 does on `http.js`
- `adminAuthApi.js` — the admin MFA login/logout calls (`/api/v1/admin/auth/*`)

**Admin console auth (`AdminAuthContext`, `pages/AdminLogin.jsx`):** `/admin` requires two layers — the existing `ProtectedRoute adminOnly` (regular `User` with `role: 'admin'`) still gates who can see the dashboard shell/nav (`DashboardLayout` is unchanged and still keyed off the regular `AuthContext`), and `AdminSessionGate` additionally requires an `AdminUser` + TOTP-MFA session (established at `/admin/login`, itself behind `ProtectedRoute adminOnly`) before any admin data loads or mutates — closing the gap where a stolen regular-session cookie alone used to be enough to grant roles, mint accounts, or approve payments. `AdminAuthProvider` is mounted app-wide (cheap — only reads `localStorage` on mount) so the gate/login page can use it regardless of route nesting.

**Routing** (`App.jsx`): All page components are lazy-loaded with `React.lazy()` + `Suspense`. Route hierarchy uses hub pages (`/courses`, `/tools`, `/resources`, `/academy`) with nested detail routes. Old flat URLs have redirect entries. `RoutePrefetcher` (mounted in `App.jsx`) prefetches a route's chunk ahead of navigation — on link visibility (idle-scheduled `IntersectionObserver`) or on `touchstart`/`pointerdown` — using the path→import map in `routePreloadMap.js`; this closes the serial "entry bundle finishes → then route chunk starts" gap that mobile field testing showed adding 700ms-1.5s+ per navigation on slow connections.

**i18n:** Custom `LangContext` supports 6 languages (en/ar/it/es/de/fr). Strings live in `i18n/` files. Arabic font (Amiri) is lazy-loaded on idle (`utils/loadArabicFonts.js`) to avoid blocking initial render — except on Arabic-text-heavy pages (`Quran.jsx`, `Teachers.jsx`), which call `loadArabicFontsNow()` immediately on mount instead of waiting for idle, since the fallback→Amiri font swap was measured as a real source of layout shift (CLS) on those pages under throttled mobile networks.

**CSS:** `styles.css` is the global index (`styles/tokens.css`, `global.css`, `layout.css`, `auth.css`, `pages.css`, `components.css`, `design-system.css`, `dark.css`, `responsive.css`). Anything only needed by specific pages is code-split — imported directly by the page/component that needs it, not added to the global index — including `quran.css`/`khatm.css`/`tasbeeh.css`/`islamic-tools.css`/`adhkar.css`/`dashboard.css` and, as of the mobile performance pass, `hifz.css` (→ `Quran.jsx` + `HifzReviewPage.jsx`) and `trust-engage.css` (→ `Home.jsx` + `Dashboard.jsx`). Coverage-API field testing showed 90-96% of the global CSS was unused on any given page before this split.

**Design system** (`styles/tokens.css` + `styles/design-system.css`): `tokens.css` is already a full 3-layer semantic token system (primitive palette → semantic `--color-*`/`--bg-*`/`--text-*`/`--border-*`/`--shadow-*`/`--radius-*`/`--space-*`/`--font-*`/`--transition-*`/`--z-*` tokens → `html.dark` overrides) and `components.css`/`dashboard-shell.css` already cover card, badge, avatar, form fields, skeleton, empty-state, and a full data-table + sidebar system — Frontend Redesign Sprint 1 audited this rather than rebuilding it, and found the real gap was a set of **interactive primitives with no generic, reusable version anywhere** (only mutually-inconsistent, hand-rolled, page/feature-specific ones existed — e.g. `.qtm`/`.exit-popup`/`.vcard-overlay` each reimplement their own modal panel with different padding/radius/shadow values; `AdminDashboard.jsx` hand-rolls its tab bar with inline styles). `design-system.css` adds: Modal/Dialog (`.ds-modal*`), Drawer (`.ds-drawer*`), Toast (`.ds-toast*`), Tooltip (`.ds-tooltip*`), Dropdown/Menu (`.ds-menu*`), Tabs (`.ds-tabs`/`.ds-tab`, including an underline variant), Accordion (`.ds-accordion*`), standalone Pagination (`.ds-pagination*`, distinct from the data-table-specific `.dt__pagination`), standalone Progress (`.ds-progress*`, distinct from the dashboard-specific `.ds-bar`), and motion-reveal utilities (`.ds-reveal`/`.ds-stagger`) reusing the keyframes already defined in `global.css` (`fadeIn`/`fadeUp`/`scale-in`) rather than redefining new ones. Every rule references an existing semantic token — no new hardcoded colors/spacing/radii were introduced, and dark-mode support is automatic (no `html.dark` overrides needed in this file at all). **This sprint is presentation-layer-only and additive**: nothing in `design-system.css` is consumed by any page yet (deliberately deferred — see the sprint report for the full design-debt list, most notably the duplicated `components.css` vs `dashboard-shell.css` "ds-" prefixed component systems, and the several ad hoc modal implementations that should eventually migrate onto `.ds-modal`).

**Landing page** (`pages/Home.jsx` + `components/{Hero,TrustBar,StatsBanner,Features,Steps,Tutors,IsnadChain,Courses,JoinCTA,Pricing,Testimonials,FAQ,Trial,Newsletter,TrustBadges}.jsx` + `styles/layout/home.css` + `styles/layout/redesign.css`): Frontend Redesign Sprint 2 audited this expecting a generic/template-feeling page to rebuild, and instead found it had already been through a comprehensive premium redesign pass — aurora-drift decorative orbs, a custom `BrandMedallion` illustration, a live social-proof activity ticker, floating stat/certification pills, a scroll-cue, and a full scroll-reveal system (`hooks/useScrollReveal.js` + `components/ui/Reveal.jsx`, with staggered `nth-child` delays already applied to course/teacher/pricing/feature grids) all already existed, all already using soft brand-tinted shadows rather than generic heavy ones, and site-wide `prefers-reduced-motion` support already existed as a single blanket override in `global.css` (not something this sprint needed to add). The one confirmed, genuinely missing piece — `Header.jsx`'s sticky glass nav had no shrink-on-scroll — was implemented (`scrolled` state + `.header--scrolled`, `header.css` + `dark.css`). No section was rebuilt from scratch; doing so risked regressing already-tuned, production code for no real gain, which the sprint's own rules explicitly forbid ("never introduce visual regressions," "reuse existing components").

**Frontend UX/UI Transformation Sprint 1 (Homepage):** a fresh, file:line-cited re-audit (not a repeat of Sprint 2's summary) found the "comprehensive premium redesign" claim mostly still holds, but the scroll-reveal system's coverage was overstated — `TrustBar.jsx`, `StatsBanner.jsx`, `JoinCTA.jsx`, `Trial.jsx`, and `Newsletter.jsx` had **zero** `Reveal` usage, and `Testimonials.jsx` only wrapped its head/video-row, not the actual featured/side-testimonial layout — roughly 6 of 15 below-the-fold sections never faded in on scroll despite the section-level pattern being fully proven elsewhere. All six were wired onto the existing `Reveal`/`useScrollReveal` mechanism (no new animation system introduced); `StatsBanner.jsx` needed a merged-ref pattern since its own count-up `IntersectionObserver` already occupied the section's ref (two independent observers on one node — one drives React state, the other only toggles classList, so they don't conflict). **A third instance of the same fake `localStorage` rating widget was found and fixed**: `Tutors.jsx`'s `StarRating` (the homepage teacher-showcase cards) had the identical "any visitor can fake-rate a tutor into their own browser's localStorage" bug already fixed twice before on `TeacherProfile.jsx` and `Teachers.jsx` — fixed identically (static display of `teacher.rating`/`teacher.reviews`; orphaned `.tc2__rate-*`/`.tc2__thanks` CSS removed). Also fixed: a real bug where `Newsletter.jsx`'s Bismillah cover class name had Arabic text embedded directly in the CSS identifier (`newsletter__guide-cover-arabicبسم`) — both the JSX and the matching CSS selector had the same typo so it accidentally "worked," but it's fixed to a clean class name; `.tst__mini` (testimonial side cards) gained the same hover-lift treatment (`translateY` + `box-shadow`) already standard on every sibling card type, which it had been missing; `DeferredSection.jsx` (used to gate ~12 of Home's sections) now shows a generic centered skeleton (reusing `components/ui/Skeleton.jsx`) instead of a bare blank `aria-hidden` div while waiting to mount. **Deliberately not touched, and documented as findings for a dedicated design-system sprint rather than risked here**: `home.css` has ~170 hardcoded px spacing values with zero `var(--space-*)` token usage (a real gap, but a full conversion sweep risks visual regressions this sprint has no screenshot tooling to verify); the homepage's icon system is fragmented across three parallel strategies (the shared `components/ui/Icons.jsx` library, raw emoji, and hand-rolled inline SVGs) with no single section using the shared library; homepage CTAs have zero click/conversion analytics instrumentation despite GA4 already being wired app-wide. All three are real, but none are "no regressions without visual verification"-safe to do blind in this environment.

**Frontend UX/UI Transformation Sprint 2 (Student Dashboard):** a fresh audit of `pages/Dashboard.jsx` (the student-role dashboard specifically, not admin/teacher/parent) found the page structurally strong — sidebar, top nav, course cards, `ProgressRing`/`DsBarChart`, empty/success states, and responsive breakpoints were all already well-executed — but surfaced a genuine "fabricated social proof" issue matching the pattern already fixed three times on the homepage: `HifzLeaderboard.jsx` rendered a 100%-hardcoded `MOCK_LEADERS` array (fake student names/ranks) with no backend query, no loading/error/empty state, and a `myRank` prop that was never actually passed. Since no backend leaderboard endpoint exists and building one is out of scope for a frontend redesign sprint, the component was deleted outright (not just fixed) — `Dashboard.jsx`'s import/usage and all `.hifz-lb*` CSS in `trust-engage.css` removed — which also reduces the sidebar from 7 to 6 competing widgets, itself a cited "kitchen sink" finding. Other fixes: `CertificateCard.jsx` hardcoded `background: '#fff'`, breaking dark mode (every sibling section already used `--bg-surface`) — swapped to the token. `useDashboardData` (`hooks/useDashboard.js`) exposed `error` but `Dashboard.jsx` never consumed it — no top-level query failure had any visible UI at all, unlike the page's own sub-widgets (`TutorReviewWidget`, `NotificationPanel`) which both already had proper error+retry states; added a non-blocking warning banner (matching the existing Expired/Days-warning banner pattern already in the file) with a "Try again" button, and extended the hook to expose a composite `refetch()`. The "My Certificates" section previously only rendered when non-empty (no empty state at all, unlike every sibling section) and the "Upcoming Classes" section had no loading state (flashed its empty state for a beat before real data arrived) — both now have a proper loading/empty/populated three-state render, reusing `components/ui/Skeleton.jsx`. `StatsBanner.jsx`'s previously-local, non-exported `useCountUp` was extracted to `hooks/useCountUp.js` (removing duplication) and applied to Dashboard's four numeric KPI stats (streak, progress %, weekly sessions, XP) — the dashboard previously had zero count-up/reveal-on-mount animation anywhere, reading as static/instant; the extraction also added a `prefers-reduced-motion` check the original never had, since a plain `setInterval` count isn't covered by the site-wide CSS reduced-motion override the way transitions/animations are. The 7 card-section headers in `Dashboard.jsx` (`.ds-card__title`) were `<span>`s, not real headings — the page had exactly one `<h1>` and then flat, unlabeled card sections for screen-reader users; converted to `<h2>` (scoped to this file only, since `.ds-card__title` is shared much more broadly across admin/teacher/parent dashboards and this sprint's scope is student-only), with a defensive `margin: 0` added to `.ds-card__title` in `dashboard-shell.css` so the tag change can't introduce a browser-default-heading-margin layout shift. **Deliberately deferred, same reasoning as Sprint 1**: `Dashboard.jsx` has 30+ hardcoded px/rem spacing and typography values despite the same `--space-*`/`--text-*` token system; `ReferralCard.jsx`'s `.referral-card` and the gold-gradient `.ds-streak` widget remain bespoke visual languages alongside `.ds-card` on the same page (`CertificateCard`'s base surface/border now uses tokens per the fix above, but its type-tinted accent border/icon background are intentional per-type theming, not a gap). Both are real, but a full sweep/consolidation risks visual regressions with no screenshot tooling to verify, so they're documented findings rather than blind edits.

**Frontend UX/UI Transformation Sprint 3 (Learning Experience):** a fresh audit of `pages/CourseContent.jsx`, `HomeworkPage.jsx`, `AttendancePage.jsx`, `CalendarPage.jsx`, and the Quran reader found the Quran module (`pages/Quran.jsx` + `components/features/quran/`) genuinely matches its extensive prior documentation — verified line-by-line, left untouched — but surfaced the largest fabricated-data finding of this engagement: **`HomeworkPage.jsx` and `AttendancePage.jsx` have no backend at all** (no `homeworkRoutes.js`/`attendanceRoutes.js`, no `Homework`/`Attendance` models exist anywhere in `backend/`) — every assignment, grade, student roster, and attendance record shown is fabricated client-side fallback data (`try { real call } catch { return hardcoded/random data }`, guaranteed to hit the catch branch), and Submit/Grade/Mark-Attendance mutations silently swallowed the guaranteed failure and called `onSuccess` anyway — reporting fake success for actions that saved nothing. Per explicit user direction (building real backend support was ruled out of scope — no inventing APIs), both pages keep all existing UI/functionality intact but now (a) show a shared `components/ui/PreviewBanner.jsx` at the top stating the data is illustrative and actions aren't persisted, and (b) let the mutations' guaranteed failure propagate instead of swallowing it, surfaced via a dismissible "wasn't saved — this page is a preview" banner — honest failure instead of fake success, not a redesign of the pages themselves. **`CalendarPage.jsx` got an actual fix, not just a banner**: unlike Homework/Attendance, a real, working, role-aware live-class endpoint already exists (`GET /api/classes` → `LiveClass` model, the exact same endpoint `Dashboard.jsx`'s "Upcoming Classes" card already consumes correctly) — `CalendarPage.jsx` was calling a different, nonexistent `/api/sessions` endpoint and silently regenerating 18 random fake sessions with invented teacher names on every load, with a dead `href="#"` "Join Session" link. Rewired to call the real `getClasses()` (`api/classApi.js`) via a new `utils/calendarHelpers.js` (`mapLiveClassToEvent`/`inferSessionType`/`platformFromUrl` — `LiveClass` has no subject-type/platform field, so a type is inferred from the free-text title via keyword matching rather than invented, and platform is derived from the real `meetingUrl`), which also makes "Join Session" work for real and adds a genuine loading-failure retry state (the calendar previously had no error UI at all). The stats strip's "Absent" tile (a status `LiveClass` doesn't have) was replaced with "Cancelled" (a real status value). **`CourseContent.jsx`** (the actual real, `getCourse`/`getCourseProgress`-backed lesson page — confirmed NOT part of the fabricated-data pattern) had three genuine, evidence-based gaps fixed: no skeleton while loading (plain loading text, unlike every other page audited this sprint) — now uses `components/ui/Skeleton.jsx`; no way to jump back to where a student left off across a long flat lesson list — added a "Continue learning" card that scrolls to and briefly highlights the first not-yet-completed lesson; and youtube/video lessons only ever opened a new tab — added an inline embedded player (`utils/courseLessonHelpers.js`'s `getYouTubeEmbedUrl` parses recognized YouTube URL shapes into an embed URL, falling back to the original new-tab link for anything unparseable or for direct `<video>` sources) toggled the same way the existing text-lesson "Read" button already worked, so no new interaction pattern was invented. `CertificateCard.jsx`'s "Download" button was mislabeled (it opens a print dialog via `document.write()`+`window.print()`, it never downloaded a file) — relabeled "Print / Save PDF," and a null `window.open()` return (popup blocked) now shows a message instead of throwing on the next line. **Deliberately out of scope, documented rather than built**: `CalendarPage.jsx`'s "+ New Session" button has no `onClick` at all (a pre-existing dead button) — building a real teacher-facing session-creation modal is new feature work, not a fix to something existing, so it's left as a finding for a future feature sprint rather than attempted here.

**Brand mark replacement + Homepage Visual Redesign Sprint:** the user supplied a new logo design (an open book with a mosque dome + minaret rising from the spine, on a rounded green tile) via a reference screenshot rather than a usable asset file, and asked for it "used everywhere." Auditing actual brand-mark usage (not assuming) found **three independent, inconsistent implementations coexisting**: `components/layout/Brand.jsx` (a `۩` Unicode glyph — used by Header, Footer, Login, Register, AdminLogin, `PageBar.jsx`, `QuranTopBar.jsx`), `DashboardLayout.jsx`'s sidebar (a bespoke inline `ر` glyph in its own gold tile, entirely separate from `Brand.jsx`), and `BrandMedallion.jsx` (Hero's large animated 8-point Rub-el-Hizb star — confirmed already "genuinely premium" by a prior sprint's audit, deliberately left untouched rather than reskinned, since it isn't literally the small logo mark and redesigning an already-excellent component wasn't asked for). Built one new canonical `components/ui/BrandIcon.jsx` (a `useId()`-scoped SVG so multiple simultaneous instances — e.g. Header + Footer both mounted at once — never collide on gradient IDs) and pointed `Brand.jsx` and `DashboardLayout.jsx`'s sidebar at it, so every one of those seven-plus locations now renders the same mark from one source. `ForgotPassword.jsx`/`ResetPassword.jsx` had no brand mark at all (unlike `Login`/`Register`) — added `<Brand />` matching the existing pattern. Static assets needing the same mark outside React (`favicon.svg`, `index.html`'s first-paint loading screen, `og-cover.svg`) were hand-updated with equivalent raw SVG. `logo.png` (only ever referenced from schema.org JSON-LD, never rendered in the UI) and `og-cover.jpg` (the actual social-preview raster) can't be produced from React or hand-written SVG — regenerated via a **temporary, scratch-directory-only** `sharp` install (never added to `frontend/package.json`) that rasterized the new design, then removed; the icon's dome geometry was iterated by actually rasterizing and viewing each draft (an early version read as a "bottle/lamp" rather than a mosque dome) before finalizing — the one part of this sprint with genuine visual verification, unlike the rest of the redesign. Beyond branding, an audit-while-fixing pass found `TrustBadges.jsx`'s top stat-card icons were raw emoji (🎓⚡🌍🛡️) while its own badges row two dozen lines below already used clean inline SVGs matching `Features.jsx`'s icon language — replaced with matching SVGs (reusing `Features.jsx`'s exact Globe and Shield-check paths) in a gradient icon-wrap circle with a hover scale, closing that one inconsistency. Also found and fixed a **genuine full duplication**: `styles/trust-engage.css` and `styles/layout/home.css` each independently defined the entire `.trust`/`.trust__card`/`.trust__badge`/`.trust__countries`/`.trust__flags` rule set (TrustBadges' styling) with slightly different values — consolidated onto `home.css` (already updated with the new icon treatment) and removed the stale duplicate block from `trust-engage.css`, keeping only what's genuinely unique to that file (`TrustBar`'s GDPR tag, the Quran audio player widget, pricing refund banner, etc.). **Scope note**: this sprint's own validation is code-level only — no browser/screenshot tool exists in this environment to render the live app, a constraint surfaced to the user before starting, who accepted proceeding on code-level reasoning with their own visual QA on the preview deploy. Given that constraint plus two prior sprints having already exhaustively audited and refined this same homepage, the redesign pass here deliberately targeted concrete, describable, low-regression-risk fixes (branding consolidation, icon-language consistency, CSS de-duplication) rather than a blind full rebuild of every section's layout — Hero/Stats/Teachers/Courses/Testimonials/Pricing/FAQ/CTA/Newsletter structures were left as-is, having already been vetted premium by prior sprints' audits.

**Correction from user visual QA — `BrandMedallion.jsx` was wrong to leave untouched.** A subsequent QA pass against real screenshots (external reviewer, not this session) found the Hero's `BrandMedallion` — an 8-point Rub-el-Hizb star in **blue/gold** with Arabic "الرَّحمة" — was completely unrelated to the new green/gold book+dome identity applied everywhere else, i.e. the single most visually prominent element on the homepage was still the *old* brand. The earlier reasoning ("already confirmed premium by a prior audit, isn't literally the small logo mark") was a scoping mistake — "premium execution" and "on-brand" are different questions, and the QA correctly caught that this component failed the second one. Fixed: `BrandMedallion.jsx` now renders the same book+dome+minaret mark as `BrandIcon.jsx` (scaled up, positioned above the existing Arabic wordmark) inside its existing animated ring/octagon frame — engineering (the `useId()`-scoped gradients, the rotation animation, the octagon geometry) was kept, only the colors (blue→green, matching the site palette) and the central icon (star→book+dome) changed. Also fixed while addressing this: `Hero.jsx`'s two floating pills used raw emoji (⭐, 🎓) — replaced with inline SVG in a gradient circle badge, consistent with `TrustBadges.jsx`'s icon treatment from the same sprint. A QA claim of a "duplicate Login button" in the header was checked against the actual code and does not reproduce — `Header.jsx` has exactly one `/login` link, conditionally rendered only when logged out — so no change was made there; the QA screenshot likely reflected a different (older, pre-engagement) deployment. **Operationally important, since resolved (see Round 5's Deployment Audit below)**: at the time of this sprint, every sprint's work had been merged into `feat/notification-system`, which had never been merged into `main` — meaning none of this engagement's work was actually live yet. This is why visual QA against the production domain kept finding pre-engagement branding.

**Round 5 — Deployment Audit: `feat/notification-system` merged into `main` for the first time.** `PR #18` had gone `CONFLICTING` (`gh pr view --json mergeable`) because `main` had drifted independently by 10 commits (two hotfixes — `fix/render-backend-url`, `fix/admin-ip-whitelist-trust-proxy` — plus a large backend/frontend audit-report commit that also included an accidentally-tracked `frontend/coverage/` directory, ~32k lines of generated HTML coverage output that was never gitignored). Resolved by merging `main` into `feat/notification-system` locally: only 2 files actually conflicted (`NotificationPanel.jsx` + its test) — `vercel.json` (carrying the Render-backend-URL fix) auto-merged cleanly. The `NotificationPanel.jsx` conflict was two independent implementations of the same read-state-persistence fix (`main`'s narrower version vs. `feat/notification-system`'s fuller rewrite with loading/error states, pagination, and click-to-navigate) — resolved in favor of the fuller version, which already was a superset including main's core fix, verified by running its test file (10/10 passing) before completing the merge. Also removed the tracked `frontend/coverage/` directory and added `coverage` to `.gitignore` in a separate commit, since it was clearly unintentional build-artifact noise, not source. Full validation (frontend lint, 187 frontend tests, 390 backend tests, production build) passed on the merged branch before pushing; PR #18's own CI (this was the first PR in the whole engagement to target `main` directly, so it's also the first to run the real `Backend`/`Frontend (lint+test+build)` GitHub Actions jobs, not just Vercel preview checks) passed green, and the PR was merged into `main`. **Verified empirically, not assumed**: the `alrahma` Vercel project already has GitHub-integration auto-deploy configured — merging to `main` alone triggered a new production deployment within ~2 minutes with no manual `vercel deploy` needed, confirmed via the merge commit's own `Vercel – alrahma` GitHub commit status pointing at the new deployment ID, which matched the one aliased to `al-rahmaacademy.com`. Confirmed the live production site is actually serving this code (not cached/stale) by fetching `https://al-rahmaacademy.com/assets/Home-*.js` directly and finding `course-row`/`tc3__card`/`tc3__spotlight` — the Round 3 (Courses accordion) and Round 4 (Teachers spotlight) class names — present in the live bundle, with the JS chunk hash matching a local build from the same merged commit exactly.

**Round 2 — remaining emoji sweep across the homepage.** Following the iterative approach above, this round targeted the homepage sections still showing raw emoji instead of the site's inline-SVG icon language (Lucide-style, `stroke="currentColor"` or matching the Features.jsx white-stroke treatment) — the clearest, most evidence-based "still looks template-like" tell available without a rendering tool, since it's a concrete presence/absence check, not a subjective layout judgment. Fixed: `Courses.jsx`'s course-card banner icons (📖🎙️🧠📜🕌🔤 — the single largest visual element on each card) replaced with custom SVG (book, microphone, repeat-arrows for Hifz repetition, award/ribbon for Ijazah, the same mosque-dome mark as `BrandIcon`/`BrandMedallion` for Islamic Studies, and a text/type glyph for the alphabet course) — iterated via the rasterize-and-view technique before wiring in, same as the logo and Hero medallion. `Tutors.jsx`'s credentials-bar icons (🎓📜👩‍🏫🌍), `JoinCTA.jsx`'s stats-row icons (🌍⭐🎓🎁) and guarantee-badge shield, `Newsletter.jsx`'s benefits-list icons (📖🎧🗓️), and `Testimonials.jsx`'s course-label book icon (2 occurrences) all replaced the same way, reusing identical path data across files where the same concept repeats (globe, graduation-cap, shield-check, book) rather than drawing a new icon per file. `Footer.jsx` had exactly one stray emoji (🛡️ next to the Refund Policy link, the only non-icon-component element in an otherwise `Icons.jsx`-based footer) — fixed the same way. **Deliberately not touched**: `Steps.jsx`/`IsnadChain.jsx` also use emoji icons per earlier audits, but weren't in this round's named scope (Trust/Features/Teachers/Courses/Testimonials/CTA/Newsletter/Footer) — flagged for a future round rather than assumed in-scope. Footer's overall structure (4-column link grid + trust-badge strip + bottom bar) was audited and found to already match standard premium-SaaS convention (Stripe/Vercel do the same) rather than reading as a generic template — left structurally unchanged.

**Round 3 — Courses section structural redesign (not another polish pass).** Explicitly scoped by the user to pick exactly one still-weak homepage section and challenge its layout/hierarchy/interactions, not its icons or colors (those were Round 1/2's job). Selected `Courses.jsx`: it was the only one of the three candidate sections (Courses, Teachers, Testimonials) presenting all six courses as visually interchangeable in a uniform 3-column card grid, with no hierarchy signal for the site's actual flagship offering (Hifz/memorization) and a "browse everything or nothing" interaction model (every card's full description always visible, or none of it, no way to scan titles first). Rebuilt as a vertical accordion list (`.courses__list` of `.course-row` items): one course expanded at a time via a CSS `grid-template-rows: 0fr → 1fr` height transition (no JS height measurement, no hardcoded max-height), defaulting open on the course flagged `popular: true` in `COURSE_META` (Hifz) so the richer expanded state is visible on first paint, not only after a click — the same "highlighted plan" hierarchy pattern already used on the pricing table, applied here for the first time. Each row header now shows icon + title + a "Most Popular" badge (where applicable) + level/duration badges + a rotating chevron, so users can scan all six titles at a glance before committing to reading any one description — the old grid required reading (or ignoring) all six full card bodies with no scan-first option. Added a secondary "Browse full curriculum →" link to `/courses` in the section head, giving the section an explicit escape hatch to the full catalog instead of only the six homepage cards. Removed the `MobileCarousel` wrapper (confirmed via grep it's still correctly used elsewhere, e.g. `Tutors.jsx` — it was only needed for horizontal-swipe card grids, not a vertical accordion). `handleStart()`'s per-course-type navigation logic (alphabet-inline-expand, detail-path routing, resource picker, Quran-reader redirect, trial scroll-and-highlight) is unchanged — this was a layout/interaction redesign, not a behavior change. CSS selectors renamed throughout (`home.css`, `dark.css`, `rtl.css`) from the old `.course`/`.course__banner`/`.course__body`/`.course__points`/`.course__badges` card-grid names to `.course-row`/`.course-row__icon`/`.course-row__body`/`.course-row__points`/`.course-row__badges`; `.course__link` (the CTA button class) was deliberately kept unchanged since it's a leaf element, not a structural one. Round 2's SVG icon set (`COURSE_ICONS`) and `COURSE_META` gradients were reused as-is inside the new row icon slot — this round is additive to Round 2's icon work, not a redo of it.

**Round 4 — Teachers section structural redesign.** Following Round 3's precedent (one section, genuine structural challenge, no icon/color-only polish), this round targeted `Tutors.jsx` (the homepage Teachers section — the second of the three candidates named across Rounds 3/4). The old `.tc2__card` grid gave all ten tutors identical visual weight in a uniform 4-column vertical card (colored block + centered avatar on top, full body below), with no hierarchy signal despite the data already containing a clear standout (Khairiyya Al-Muhammadi, the only 5.0-rated tutor). Rebuilt around two new pieces: a **spotlight card** (`.tc3__spotlight`) — computed via `useSpotlightTeacher()` (sorts by rating, then review count, purely in-component; `data/teachers.js` itself was not touched) — presenting the top-rated tutor full-width with their bio excerpt (data already existed but was never surfaced on the homepage), a large avatar, and the full specialties list, using the same "highlighted plan" gold-accent treatment already established by Pricing and Round 3's Courses accordion; and a **redesigned card** (`.tc3__card`) for the remaining nine, changed from vertical (avatar-on-top) to a horizontal profile-row layout (avatar column + info column), matching common tutor-marketplace convention (Preply/italki/Wyzant) rather than a generic stacked React card. Avatar treatment changed from a plain initials-in-circle to a colored gradient ring with a dedicated verified-checkmark badge overlay (`.tc3__verified`, carrying the existing `tp.alazharBadge` text as its accessible label/tooltip rather than inventing new copy or touching the shared i18n string also used by `TeacherProfile.jsx`). Rating moved from a full star-row + count in the body to a compact corner pill (`.tc3__rating-pill`) next to the name for at-a-glance trust, with reviews/hours demoted to a small icon-led meta row. CTA hierarchy fixed: one solid primary "Enroll" pill button plus a de-emphasized text "View Profile" link, replacing two equal-weight `.btn--ghost`/`.btn--green` buttons. Removed the `MobileCarousel` wrapper (Tutors.jsx was one of its two remaining consumers after Round 3 removed it from Courses; `Pricing.jsx` is now the only user) — the new horizontal card works as a plain responsive grid (3 → 2 → 1 columns) without a swipe carousel, with dedicated breakpoints added to `responsive.css` to reflow the grid and stack the spotlight card on narrow screens. **Incidental fixes surfaced while editing the shared stagger/responsive CSS files** (not part of this round's brief, but genuine pre-existing defects directly in the files being touched): `layout/footer.css` still had Round 3's pre-rename `.courses__grid .course:nth-child(n)` stagger-delay selectors and `.courses__grid`/`.tc2__grid` responsive breakpoints — orphaned since Round 3 renamed those classes and missed this file, meaning Courses/Teachers cards had silently lost their staggered scroll-reveal timing and one duplicate/conflicting responsive breakpoint; renamed to the current `.courses__list .course-row`/`.tc3__grid` selectors. `dark.css` had a `.tc2__name`/`.tc2__spec` dark-mode override that never matched any real class in `Tutors.jsx` (a typo predating this engagement) — meaning tutor names never actually got a dark-mode-safe color and would have rendered `--green-deep` (a near-black green with no dark-mode override) on a dark background; fixed to the correct `.tc3__name-ar` selector. `layout/redesign.css` had a `.tc2__actions .btn` rule that only made sense for the old two-equal-button layout — removed as dead weight now that actions use dedicated `.tc3__cta`/`.tc3__cta-link` classes.

**Round 5 — remaining 7 homepage sections (Hero, Features, Testimonials, Pricing, FAQ, CTA, Newsletter).** Scoped explicitly to skip Courses/Teachers (just rebuilt in Rounds 3/4) and cover the rest in one pass, per user direction after being asked to weigh one-big-pass vs. continuing the iterative cadence. Weakest sections got real structural rebuilds; already-strong ones (Hero, CTA) got targeted, concrete additions rather than a risky full rebuild, consistent with this engagement's standing rule not to redo already-tuned, production-proven sections for no real gain. **FAQ** (the plainest section — a bare bordered accordion with a text `+`/`−` glyph, no icons, no animation): rebuilt as a numbered accordion with a smooth `grid-template-rows` expand (same technique as the Courses accordion), an icon that rotates into an "×", and a new sticky "Still have questions? Chat on WhatsApp" aside card — the first section on the homepage with a persistent secondary contact path. **Features**: replaced the uniform 3-up centered-icon card grid (the single most "generic AI template" pattern on the page) with an asymmetric bento grid — the Free Trial card (the conversion-critical one) spans 2 columns with left-aligned icon+heading, larger type, and a new "Start your free trial →" link to `#trial`; the other five became compact left-aligned tiles instead of centered-stacked ones. **Pricing**: removed the solid-color gradient header block (the same "colored box on white card" pattern Teachers/Courses had before their redesigns) in favor of a unified card with an icon badge (replacing 🌱⭐👑 emoji with SVG), price integrated into the body, and the featured plan gets a gold-tinted background wash instead of just a border — checkmarks became real SVG bullets instead of `content: "✓"` text. **Hero**: left the already-premium orbs/medallion/floating-pills/activity-ticker structure alone (per the standing rule) but added concrete gaps — a larger hero-specific h1 clamp for more impact, icons on the stats bar (previously plain numbers), and a decorative quote mark on the verse card matching Testimonials' existing pattern. **Testimonials**: fixed the one remaining raw-emoji spot (🧒👩👨‍👩‍👧 video-testimonial thumbnails) with initials-in-gradient-circle, matching the avatar convention the rest of the section already uses. **CTA**: the stats row went from plain inline text to glass pill-cards with hover lift, matching the card language used everywhere else. **Newsletter**: replaced the 📬 success-state emoji with an SVG envelope + checkmark badge. **Incidental fixes found while touching shared CSS** (same discipline as Round 4): a duplicate `[dir="rtl"] .plan__tag` rule in `rtl.css` (identical rule existed twice); a now-stale `[dir="rtl"] .plan li::before` margin override left over from before `.plan li` switched to flexbox `gap` (would have added an unwanted extra margin); a `[dir="rtl"] .faq__question { flex-direction: row-reverse }` tuned for the old 2-child structure that would have mis-ordered the new 3-child (number/text/icon) layout — removed in favor of flexbox's native RTL mirroring; and a `responsive.css` 320px rule (`.plan__price span`) orphaned by the price markup rename, updated to `.plan__price-amount`. `data/teachers.js`, `data/socialProof.js`, and all handler logic (`handleStart`, mutation calls, form submit) were untouched — this was a visual/structural pass only, per the mission's "keep existing functionality" constraint.

**Post-Round-5 fix: real screenshot verification (Playwright, `@playwright/test` + Chromium already installed in this repo) caught 5 genuine visual bugs that pure code review had missed** — the first time in this entire engagement that the live rendered site could actually be looked at, rather than reasoned about from source, at both desktop and mobile viewports. (1) **Collapsed accordion items leaked a line of answer text** in both the FAQ accordion (new this round) and the Courses accordion (Round 3) — the same root cause in both: the directly-collapsing grid item (`.faq__answer` / `.course-row__body`) carried its own bottom padding, and `grid-template-rows: 0fr` only zeroes the *track*, not the item's own padding — `overflow: hidden` clips at the item's border-box edge, which still includes that padding, so the overflowing paragraph peeked through by exactly the padding amount (empirically confirmed via `getBoundingClientRect()`: both computed to a non-zero height equal to their padding, not 0). Fixed by moving the padding one level deeper onto a new non-grid-item wrapper (`.faq__answer-inner`, `.course-row__body-inner`) so the grid item itself can reach a true 0px; verified fixed via a rebuilt local `vite preview` (0px confirmed, then a visual screenshot showing a clean collapse). (2) **Hero's badge list showed a doubled checkmark** — `.hero__badge-check`'s styled circle already renders "✓", but the `badge1`/`badge2`/`badge3` translation strings *also* start with a literal "✓ " (used verbatim elsewhere as plain inline microcopy) — fixed by stripping the leading checkmark when rendering inside the styled-badge context, without touching the shared i18n strings. (3) **JoinCTA's `promise` line still had a raw 🕌 emoji** baked into its translation string (missed by every prior emoji-sweep round since it's mid-string, not a standalone icon slot) — replaced with the same mosque-dome mark used by `BrandIcon`/Courses' Islamic Studies icon. (4) **Hero's new stats-bar icons broke mobile layout**: adding an icon column to each stat gave `.hero__stat` a wider min-content size (icon + an unbreakable word like "COUNTRIES"), and since flex items don't shrink below content size by default, the 4-across row forced `.hero__text` — and everything in it, including the h1 and paragraph — wider than its mobile column, clipping them at the viewport edge instead of wrapping (confirmed via `getBoundingClientRect()`: the text column measured ~431px inside a ~350px parent). `min-width: 0` alone wasn't sufficient (unbreakable label words still didn't fit); fixed with a ≤640px breakpoint wrapping the stats into a 2×2 grid instead of 4-across. (5) **Features' new bento layout clipped "Multilingual Instruction" on mobile** — the existing pre-Round-5 `@media (max-width: 640px)` breakpoint force-fits the grid to 2 columns via `!important` (fine for the old uniform cards, too narrow for the new icon+heading row to fit an unbreakable 12-letter word), so the heading was clipped rather than wrapped. Fixed by stacking `.feature__row` into icon-above-heading at that same breakpoint, giving the heading the full card width. All five fixes validated with real before/after screenshots (desktop and 390px mobile viewports) against a locally rebuilt `vite preview`, not just code reasoning.

**Architecture cleanup** (Frontend Architecture Sprint 3): a full dead-code/duplication audit (verified via grep cross-referencing every export against its usages, not assumed) found the frontend already in good shape — no dead hooks, no dead utils, no duplicate API functions, and the 6 i18n language files already in sync (line counts within 5 of each other). Two genuinely dead artifacts were removed: `components/ui/EmptyState.jsx` (illustrated empty-state component, zero imports anywhere, dating to a pre-engagement bulk commit) plus its now-orphaned `.empty-state*` CSS in `components.css`; and, inside `components/ui/Icons.jsx`, 13 individually-unused icon exports (`SunIcon`, `ChevronRightIcon`, `ChevronLeftIcon`, `MenuIcon`, `XIcon`, `UserIcon`, `DashboardIcon`, `TrashIcon`, `PlusIcon`, `CheckIcon`, `ExternalLinkIcon`, `SidebarIcon`, `WhatsAppIcon`) plus the `ICON_MAP`/`Icon` name-based lookup wrapper they only existed to support (that wrapper itself had zero consumers — every real usage imports specific named icons directly). Also consolidated a small, genuine, mechanically-verified duplication: `hooks/useEscapeKey.js` now centralizes the "call a callback when Escape is pressed while a flag is true" effect that `Header.jsx` (mobile drawer) and `QuickTrialModal.jsx` each hand-rolled separately — it reads the callback from a ref internally, so passing a fresh inline arrow function every render (the normal case) doesn't tear down and re-attach the `keydown` listener the way a naive extraction would. **Deliberately not touched**: the confirmed, larger duplication from Sprint 1 (`components.css`'s generic card/badge/skeleton/table system vs `dashboard-shell.css`'s parallel "ds-" prefixed one; 9+ ad hoc modal implementations that should eventually migrate onto Sprint 1's `.ds-modal`) — consolidating those means changing the rendered output of many live pages, and this sprint has no way to visually verify "no regression" for that (no browser/screenshot tooling available), so it's left as documented technical debt for a sprint that does.

**Brand-mark motion audit (post brand-mark-replacement cleanup):** the user asked for a full re-verification that "every old logo/animation" was gone and the mark presented as clean/minimal — an audit of the actual current state (not an assumption) found the book+dome+minaret `BrandIcon`/`Brand`/`BrandMedallion` system from the earlier "Brand mark replacement" sprint was already the single source of truth everywhere (Header, Footer, Login, Register, AdminLogin, ForgotPassword, ResetPassword, PageBar, QuranTopBar, `DashboardLayout` sidebar, favicon.svg, manifest.json, og-cover.svg/jpg, logo.png all confirmed already showing the correct green-tile mark), so this pass was a motion/consistency cleanup rather than another brand rebuild. Removed: `BrandMedallion.jsx`'s 60s `med-spin` outer-ring rotation (and the `animated` prop that drove it — the medallion is now always static; `Hero.jsx` updated to match); a stale **blue** `drop-shadow` glow + hover-scale on `.hero__medallion` in `home.css` (a leftover from the pre-replacement blue/gold star medallion that was never updated when the mark's colors were fixed — replaced with a static, brand-green-toned shadow, no hover motion); the hover-scale `transition: transform` on `.brand__mark` in `header.css` (and its now-unneeded `prefers-reduced-motion` override); and the `alr-pulse` opacity-pulse animation (plus its now-dead keyframe) on the first-paint loading mark in `index.html` — the loading spinner/progress-bar below it were left alone since those are generic loading indicators, not part of the logo itself. Also removed genuinely dead old-branding CSS: `.sb__brand`/`.sb__brand-link`/`.sb__brand-icon`/`.sb__brand-text*` in `components.css` — a text/emoji-styled brand icon (`font-size: 1.5rem` glyph, not an SVG) belonging to a `.sb-*` sidebar system fully superseded by `dashboard-shell.css`'s `ds-*` sidebar and confirmed to have zero remaining JSX consumers, including its orphaned `.sb--collapsed .sb__brand-link` override. Two brand-reference gaps outside the frontend were closed: `backend/config/emailTemplates.js`'s email header used a 🕌 emoji as a stand-in "logo" — replaced with an `<img>` of the real hosted mark (`{CLIENT_URL}/logo.png`, table-based layout for email-client safety, with the "AL-Rahma Academy" text remaining as the fallback if images are blocked); and `CertificateCard.jsx`'s print/PDF certificate template had no visual mark at all (Arabic text only) — added the same inline SVG brand mark above the wordmark. Also fixed an unrelated broken asset reference found in the same audit: `PrayerTimesPage.jsx`'s prayer-alert `Notification` was pointing at `/favicon.ico`, a file that doesn't exist in `public/` (only `favicon.svg` does) — repointed to `/favicon.svg`. **Deliberately left alone**: `MilestoneCelebration.jsx`'s 🕌 emoji inside WhatsApp/clipboard share-text strings — decorative text copy in a share message, not a rendered logo asset, so out of scope for a brand-mark cleanup.

**Brand system removal — Phase 1 of a planned re-brand (icon graphic stripped, text/nav preserved, no replacement yet).** Ahead of a new logo, the user asked for the entire book+dome+minaret mark removed everywhere with zero placeholder, explicitly deferring the redesign to a later Phase 2. `components/ui/BrandIcon.jsx` and `components/ui/BrandMedallion.jsx` were deleted outright (an Explore-agent audit + manual verification confirmed zero test files import either directly, so deleting them was safe once their import sites were updated). Per explicit user direction, only the icon graphic was removed — every text label (`site.name`/tagline in `Brand.jsx`, "Al-Rahma"/"Academy" in `DashboardLayout.jsx`'s sidebar, "AL-Rahma Academy" in `InvoiceModal.jsx`/`CertificateCard.jsx`/emails) and every `<Link>` navigation wrapper (Header, Footer, PageBar, QuranTopBar, all 5 auth pages, sidebar home-link) stays fully functional — `Brand.jsx` alone controls 9 of the 13 usage sites, so trimming just its `<BrandIcon>` element propagated everywhere automatically. `BrandMedallion`'s Hero usage was removed in full (its wrapping `.hero__visual--medallion` div and the two floating trust pills beside it were left in place, now around an empty visual slot) since the user explicitly named "Medallion*" for full removal, not just its icon. The two other hand-duplicated copies of the same SVG path data — the `index.html` first-paint loading screen's `.alr-mark` and `CertificateCard.jsx`'s `.cert__mark` — were stripped the same way (icon only, surrounding text kept), since neither imports `BrandIcon` and both would otherwise have kept rendering the old mark. Every static/dangling reference was cleaned up in the same pass rather than left to 404: `favicon.svg`/`logo.png`/`og-cover.svg`/`.jpg` were deleted, and every tag pointing at them was removed too — `<link rel="icon"/apple-touch-icon/mask-icon>`, `og:image`/`twitter:image` meta (in `index.html` and its `fr/`/`it/` locale mirrors), the JSON-LD `logo`/`image` fields in both `index.html`'s site-wide schema and `BlogPost.jsx`'s per-post Article schema (the latter was a gap the plan's own audit missed and only caught in the post-implementation grep sweep), `manifest.json`'s icon arrays (emptied, not repointed), `sw.js`'s `STATIC_PRECACHE` entry (a real fix, not cosmetic — `cache.addAll()` throws if any listed URL 404s, so leaving it would have broken the service worker's install event entirely), and `PrayerTimesPage.jsx`'s `Notification` icon option. `useSEO.js`'s `DEFAULT_IMAGE` fallback was removed too, with `img = image || null` relying on the hook's existing `setMeta()` no-op-on-empty guard so pages that don't pass their own `image` simply stop emitting an `og:image` tag rather than pointing at a deleted file. Orphaned CSS (`.brand__mark`, `.hero__medallion`, `.ds-brand__logo`, `.inv__logo`, including their `responsive.css` breakpoint overrides) was removed while every sibling layout/text rule (`.brand`, `.brand__text*`, `.hero__visual--medallion`, `.ds-brand`/`.ds-brand__text*`, `.inv__header`/`.inv__academy*`) was left untouched, since none of those style the icon. Verified via a clean `npm run build`, a repo-wide grep sweep (zero remaining functional references — only two harmless code comments in `Courses.jsx`/`JoinCTA.jsx` still name-check the deleted `BrandIcon` in a doc comment on their own independent, unrelated mosque-dome content icons, deliberately left alone as out-of-scope), and live screenshots of Header/Login/Register/Hero showing text-only or an empty visual slot with zero network 404s. **Explicitly deferred to Phase 2 per the user's instruction**: no new logo, no placeholder, no layout redesign — this pass only removes.

**Brand system re-application — Phase 2 (official brand guideline implemented, no source files available).** Following Phase 1's removal, the user supplied a brand guideline composite (App Icon/Vertical/Horizontal Logo/Favicon panels, palette `#0C3834`/`#1E6B5C`/`#D4AF37`/`#F6F1E6`/`#0F1417`, Cinzel English wordmark, Cairo Arabic wordmark, calligraphic Bismillah) as a pasted image only — no vector/source file existed on disk, and no rasterization/vectorization tool is available in this environment (confirmed: no `sharp`, no ImageMagick). Per the user's explicit override ("do not wait for source files, redraw by eye, only report a blocker if literally impossible"), every asset was hand-authored as SVG directly from visual analysis of the guideline, iterated by rendering to PNG via a Playwright screenshot harness and re-adjusting shape/gradient/proportions before wiring anything in (the dome path went through two revisions — the first read as a bell/lamp rather than a bulbous mosque onion-dome; the wing gradient went through two revisions to make the green→gold two-tone actually read at a glance). `components/ui/BrandIcon.jsx` (recreated) is the single source-of-truth icon — dome/minaret/crescent/two-tone wing geometry, `tile`/transparent variants via prop, `useId()`-scoped gradients so multiple simultaneous instances never collide. A new `components/ui/BrandLockup.jsx` (+ `styles/brand-lockup.css`, Cinzel/Cairo self-hosted via new `@fontsource/cinzel`/`@fontsource/cairo` packages, loaded eagerly in `main.jsx` unlike lazy-loaded Amiri since the Header logo renders on every page) renders the full icon+wordmark+Bismillah lockup as a self-contained dark-green card (horizontal/vertical via prop) — used in `Hero.jsx`'s previously-empty visual slot per the user's explicit instruction that Hero must show the real lockup, not a custom emblem, with no rotation/glow/float animation added. **Deliberate, reported-not-hidden substitution**: the Bismillah line is rendered as the correct, real Arabic string set in Amiri (already self-hosted, already calligraphic-style, used throughout the Quran reader for the same kind of religious text) rather than freehand-traced bezier art — chosen because inaccurate hand-traced Arabic risking a garbled sacred phrase is a worse outcome than a faithful typeset rendering; everything else (icon geometry, typography choice, colors, proportions) is hand-redrawn from the guideline. Static assets built from the same path data: `favicon.svg` (icon-only, tile variant) and `og-cover.svg` (icon + wordmark at 1200×630) — the latter's first draft had the RTL Arabic/Bismillah lines colliding with the icon (SVG renders RTL text leftward from its anchor, an easy trap), fixed by center-aligning all four text lines on a shared x far enough right of the icon. `logo.png` was **not** recreated as a literal file — per the "only block on literal impossibility" rule, producing real PNG bytes is impossible without a rasterizer, so every former `logo.png` reference (JSON-LD `logo` fields in `index.html`/`BlogPost.jsx`) now points at `favicon.svg` directly instead of shipping a mislabeled fake `.png`. Applied everywhere Phase 1 emptied: `Brand.jsx`/`DashboardLayout.jsx`/`InvoiceModal.jsx` (icon re-added, and — discovered only after re-adding — the JSX/CSS plumbing in `Brand.jsx` and `header.css`'s `.brand__mark` rule round-tripped to byte-identical text vs. pre-Phase-1 `HEAD`, i.e. `git diff` shows no change there at all even though the icon artwork itself is entirely new); `CertificateCard.jsx` (inline SVG re-added above the Arabic wordmark in the print template, since that's a raw HTML string, not JSX); `emailTemplates.js` (`<img>` re-added, absolute URL via `CLIENT_URL`, header background updated from the old ad hoc `#0b6e4f` to the guideline's `#0c3834` to avoid a mixed-branding look now that the header carries the guideline-colored mark right next to it); `index.html` (favicon/apple-touch-icon/mask-icon links, og:image/twitter:image meta, JSON-LD `logo`/`image`, first-paint loading-screen inline SVG) + `fr/`/`it/` locale mirrors + `BlogPost.jsx`'s per-post schema; `manifest.json` (all 4 icon arrays repopulated with a single `sizes:"any", type:"image/svg+xml"` entry — 192/512 PNG entries are the one genuine literal-impossibility gap, flagged not built); `sw.js` (`/favicon.svg` back in `STATIC_PRECACHE`); `PrayerTimesPage.jsx` (Notification `icon` restored); `useSEO.js` (`DEFAULT_IMAGE` restored, pointing at `og-cover.svg`). **Known, reported limitations**: SVG `og:image`/`twitter:image` may not render as a link preview on Facebook/X/LinkedIn (they generally require raster) and SVG `<img>` in emails has patchy support in some desktop mail clients (notably Outlook's Word-based renderer) — both are called out in-line as code comments at their respective source, not silently shipped. Validated: `npm run lint` (0 errors), full test suite (187 frontend + 390 backend, 0 failures), `npm run build` (0 errors; one pre-existing, unrelated CSS minifier warning was confirmed via a `git stash` A/B comparison to already exist on the untouched pre-Phase-2 codebase, not something this pass introduced), a repo-wide grep sweep (zero remaining `logo.png`/`og-cover.jpg` references, every `favicon.svg`/`og-cover.svg` reference resolves and returns HTTP 200), and live Playwright screenshots of Header/Hero/Footer/Login/Register against a real dev server with zero 4xx/5xx network responses on any checked page.

**Dark mode:** `ThemeContext` toggles a CSS class on `<html>`; dark overrides are in `styles/dark.css`.

**Home page** (`pages/Home.jsx`): ~16 marketing sections. Everything below the fold (`Courses` through `Newsletter`) is wrapped in `DeferredSection` (`components/ui/DeferredSection.jsx`), which mounts its children only once an `IntersectionObserver` reports the section is about to scroll into view (600px root margin, so the mount finishes before the user actually gets there). Field testing measured 1.5s+ of main-thread blocking from mounting all sections synchronously on first render before this change.

**Quran module** (`pages/Quran.jsx` + `components/features/quran/`): premium Mushaf reading experience built on top of `api/quran.js` (quran.com API v4 + alquran.cloud). Nav modes: Surah / Page / Juz / Hizb (what content is loaded/browsed) are orthogonal to **Reading Mode**: Continuous vs. Verse-by-Verse (how it renders) — a segmented `ReadingModeSwitch.jsx` toggle, persisted to `localStorage['qlc-reading-mode']`, drives which renderer is used regardless of nav mode. Continuous mode renders via `QuranMushafPage.jsx` (flowing RTL text with ornamental frame/margins, `styles/quran-mushaf.css`) — generalized to accept any loaded verse array (single page, full surah, juz or hizb, up to ~300 verses), not just page-sized data; Verse-by-Verse mode uses the list-based `QuranVerseList.jsx`. Switching modes never refetches — both renderers read the same already-loaded `verses` state.
- **Page-turn navigation** (Continuous mode): tap zones (left/right ~22% edge bands, center toggles immersive chrome), swipe (`hooks/useSwipeNavigation.js` — hand-rolled touch-delta threshold, no gesture library), and keyboard arrows all call one consolidated `goRelative(dir)` handler in `Quran.jsx`, which reuses each nav mode's existing prev/next unit semantics (prev/next surah, page±1, juz±1, hizb±1). `useQuranKeyboard.js` no longer owns per-mode arrow-key logic directly — it just calls `goRelative`.
- **Immersive chrome**: tapping the center of the continuous reading viewport toggles `chromeHidden`, collapsing the top bar / mode switch / controls bar via CSS max-height transitions (`.qlc--chrome-hidden`); auto-resets on tab or reading-mode change.
- **Mobile sidebar**: `QuranSidebar.jsx` is an off-canvas drawer below 900px (slide-in + backdrop, opened via a `☰` toggle in `QuranTopBar.jsx`), replacing an earlier "squeeze into a 240px box above content" layout that ate vertical space needed for near-fullscreen reading.
- **Notes & highlights**: per-verse note (textarea popover) and highlight (5-color swatch picker) live in `QuranVerseList.jsx`'s action row, both persisted on the same `QuranBookmark` row (`note`, `color` fields) via the existing upsert `POST /api/quran-bookmarks` — saving either one auto-bookmarks the verse, since there's no note/highlight-only row in the schema. Highlight also renders (read-only tint) inside `QuranMushafPage.jsx`.
- **Reading settings**: `SettingsPanel` (`QuranControls.jsx`) adds a Light/Sepia/Dark reading theme (`qlc--sepia` CSS class, independent of the global `ThemeContext` dark toggle), a line-height slider, and a content-width preset (Narrow/Medium/Wide) — applied via CSS custom properties (`--reading-line-height`, `--reading-max-width`) set on the page root.

Bookmarks, reading goals/streak/history and last-read position persist via `/api/quran-bookmarks` and `/api/quran-progress` (backend models `QuranBookmark`, `QuranReadingProgress`), fetched with React Query hooks (`hooks/useQuranBookmarks.js`, `useQuranProgress.js`) — a different data-fetching approach from the rest of `Quran.jsx`, which still uses raw `useState`/`useEffect` for verse data. `useQuranAudioEngine.js` is a **separate, additive** verse-queue audio engine (Media Session integration, sleep timer, resume-on-reopen) that powers `QuranSyncPlayer.jsx` for Reading-tab verse-sync/repeat-page/repeat-selection playback — it deliberately does not replace `useQuranHifz.js`, which remains the untouched, already-proven Hifz repeat/test engine, to avoid regressing that feature. The Hifz tab's third sub-mode, "Record" (`QuranRecordingStudio.jsx` + `useQuranRecorder.js`), records practice recitations via MediaRecorder and stores them client-side in IndexedDB (`utils/recordingStore.js`, never uploaded) for toggle-playback comparison against the reciter's audio; practice stats persist via `/api/quran-memo` (`QuranMemorizationStats`).

The Reading Mode / page-turn / notes / highlights / reading-theme system above applies only to `Quran.jsx` — it is the only verse-structured reading surface in the app. `CourseContent.jsx`'s text-type lessons are plain paragraphs (no Arabic/translation duality, no audio, no verse structure) and intentionally do not use this system.

**Notifications** (`models/Notification.js`, `controllers/notificationController.js`, `routes/notificationRoutes.js`, `frontend/src/components/ui/NotificationPanel.jsx`): the read-side API (list/unread-count/mark-read/mark-all-read/delete) existed for a while, but `createNotification()` was never actually called anywhere until Feature Sprint 1 — every business event below now fires it: manual-payment approve/reject (`manualPaymentController.js`), PayPal success/failure (`paymentController.js`'s `finalizePaypalOrder`), Stripe checkout + monthly renewal (`stripeController.js` — Stripe payment *failure* has no wired notification, since there is no `invoice.payment_failed` webhook case at all yet), live class scheduled/cancelled (`liveClassController.js`), certificate issued (`certificateController.js`), a message being sent (`messageController.js` — a **separate, additional** row from `Message.readAt`, which remains its own independent unread-count system for the messaging inbox badge), review approved (`reviewController.js`), and the renewal-reminder cron (`cronController.js`, guarded by the same `renewalReminderSentFor` idempotency field the email already used, so it fires the notification and the email exactly once per billing period). `createNotification({...}, { session })` accepts an optional Mongoose session so it can be created atomically alongside the payment/subscription writes it accompanies; it silently no-ops (no throw) when `recipient` is falsy, since several sources (guest checkouts, Stripe webhooks missing `userId` metadata) legitimately have nobody to notify. Enrollment approve/reject, homework, admin-announcement broadcast, and per-user coupon-grant notifications are **not wired** — the underlying business flows either don't carry a `User` `_id` to notify (`Enrollment`/`TrialRequest` are email-keyed lead forms) or don't exist as a feature at all (no `Homework` model, no announcement-broadcast admin action). The bell badge in `DashboardLayout.jsx` polls `GET /api/notifications/unread` every 30s, independently of the pre-existing `GET /api/messages/unread/count` poll that still drives the "Messages" nav item's badge — the two were previously conflated (the bell incorrectly showed the messages count).

**Reviews** (`models/Review.js`, `controllers/reviewController.js`, `routes/reviewRoutes.js`, `routes/v1/admin/reviewsRoutes.js`): `createReview`/`getTeacherReviews`/`getCourseReviews`/`moderateReview` all worked correctly before Feature Sprint 2, but had zero frontend consumers anywhere and no admin listing endpoint at all — `moderateReview` existed with no way to discover which review `_id`s needed moderating. `GET /api/reviews` (`listReviews`, optionally `?status=`) was added on the legacy `protect+adminOnly` router — the same "admin reads stay on the legacy router, only the mutation is on the hardened `/v1/admin` MFA stack" convention already used for coupons/contact/certificates — to make the moderation queue actually usable. `getTeacherReviews`/`getCourseReviews`/`moderateReview`/`createReview` also gained the same `ObjectId.isValid()` guard pattern applied elsewhere in the admin surface (malformed ids previously threw an unhandled CastError → 500 instead of a clean 400). Frontend: `Dashboard.jsx`'s existing "My Tutor" card (real assigned teacher via `user.teacher`, a genuine `User._id` — **not** `enrollment.teacherName`, which is only a free-text label from the public lead-capture `Enrollment` form) now has a real `TutorReviewWidget` showing the live approved-review average/count and a submit-review form with a pending-approval state; `AdminDashboard.jsx` gained a "Reviews" tab (`AdminReviewsTab.jsx`) for the approve/reject moderation queue. The public marketing teacher directory (`pages/TeacherProfile.jsx`, `pages/Teachers.jsx`, `data/teachers.js`) is **not** wired to this system — it's 100% static editorial content (fictional numeric ids, hardcoded bios/ratings) with no corresponding real `User` account, so there is no real teacher/review data to connect it to without inventing a new public-teacher-directory feature; the only change there was removing a fake `localStorage`-based "rate this teacher" widget that recomputed a client-only average no one else ever saw, replaced with an honest static display of the same editorial numbers. **Course reviews**: `getCourseReviews` originally returned only `reviews/total/page/pages` (no average or distribution, unlike `getTeacherReviews`) — extended with a new `Review.ratingStatsForCourse` static (separate from `avgRatingForTeacher`, to avoid touching the already-shipped teacher-review path) so the response also carries `avg`/`count`/`distribution` (a `{1..5: count}` breakdown across ALL approved reviews for the course, not just the current page). `components/features/courses/CourseReviews.jsx` consumes this on `CourseContent.jsx` (the real, `ProtectedRoute`-gated course detail page — not the static marketing course hubs) with a submit-review form, pending-approval/already-reviewed states, and "Load more" pagination. There is no edit/delete-own-review endpoint on the backend (only create + admin moderate), so the UI intentionally doesn't offer those actions.

**Reviews & Ratings Completion (Feature Sprint 5):** a fresh re-audit found the backend/`CourseReviews.jsx` path from Sprint 2 already production-complete (creation, duplicate prevention, ownership, moderation, pagination, indexes, and 28 backend tests all held up), but found three genuine gaps. (1) `getTeacherReviews`/`getCourseReviews` gained an additive, backward-compatible `?sort=` param (`recent` default / `rating_desc` / `rating_asc`, resolved via `resolveReviewSort()` in `reviewController.js` — an unrecognized value silently falls back to `recent` rather than erroring), consumed by a sort `<select>` in both `TutorReviewWidget` and `CourseReviews.jsx` (shown only once a surface has more than one review). (2) `TutorReviewWidget` (`Dashboard.jsx`) previously showed only the average/count summary and a submit form with no actual review list — it now fetches and renders a paginated "recent reviews" list (`TUTOR_REVIEWS_PAGE_SIZE = 3`, "Show more reviews" to page in, mirroring `CourseReviews.jsx`'s existing "Load more" pattern) and a retry button on the rating-load error state (previously dead-ended with no recovery path). Both review-submission textareas (`TutorReviewWidget` and `CourseReviews.jsx`) gained a live `n/2000` character counter. (3) **A second fake `localStorage` rating widget was found and fixed**: Sprint 2's cleanup of the public teacher directory only touched `TeacherProfile.jsx` (the single-teacher detail page) — `pages/Teachers.jsx` (the teacher **listing** page, same static `TEACHERS` data source) still had its own `StarRow` component letting any visitor "rate" a teacher via a button that wrote only to their own browser's `localStorage` (`tc-rating-${teacher.id}`) and recomputed a fake average nobody else ever saw. Fixed identically to the earlier `TeacherProfile.jsx` fix: replaced with a static, non-interactive display of the same editorial `teacher.rating`/`teacher.reviews` figures (the now-unused `.tpg__rate`/`.tpg__rate-btn`/`.tpg__thanks` CSS was removed from `styles/layout/teachers-page.css`). Edit/delete-own-review remain intentionally unsupported — the backend only exposes create + admin-moderate, and adding them was out of scope per this sprint's "don't change APIs unless broken" constraint.

**Wishlist** (`models/Wishlist.js`, `controllers/wishlistController.js`, `routes/wishlistRoutes.js`, `frontend/src/hooks/useWishlist.js`): the full CRUD API and React Query hook (`useWishlist`/`useAddToWishlist`/`useRemoveFromWishlist`/`useClearWishlist`/`useIsWishlisted`) already existed — including a partial (read-only) frontend test file — but, like Notifications/Reviews before Feature Sprints 1–2, had zero real UI consumers anywhere in the app until Feature Sprint 3. Investigation also found a genuine defect in `addToWishlist`: a plain `$addToSet` does **not** dedupe here, because each embedded `courses` subdocument also carries an `addedAt` field (schema default `Date.now`) that Mongoose re-casts fresh on every request — MongoDB's `$addToSet` compares the whole embedded document, so two adds of the same course a moment apart were never equal and both got pushed, silently defeating the "set" semantics the operator is named for (verified empirically before and after the fix). Fixed with an explicit ensure-document-exists-then-conditional-`$push` pattern instead of relying on `$addToSet`. `addToWishlist`/`removeFromWishlist` also gained the same `ObjectId.isValid()` guard used elsewhere, and `addToWishlist` now 404s for a well-formed id that isn't a real `Course`. Frontend: a reusable `WishlistButton.jsx` heart-toggle is wired into every real course-card surface — `Dashboard.jsx`'s course cards and `Profile.jsx`'s "My Courses" list (both consume the same `user`'s actual enrolled/available courses, not the static marketing catalog) — and a dedicated `pages/Wishlist.jsx` (`/wishlist`, `ProtectedRoute`, linked from the student sidebar nav) lists saved courses with remove/clear-all, a count in the page heading, and loading/error/empty states. `useAddToWishlist`/`useRemoveFromWishlist` (`hooks/useWishlist.js`) apply an optimistic update to the shared `['wishlist']` query cache on `onMutate` (rolled back via the snapshotted previous value on `onError`, reconciled with the real server response via `onSettled`'s invalidate) — safe because `useIsWishlisted` (what the heart-icon fill state reads) only ever compares course ids, so a bare-id placeholder entry is enough to flip it instantly; `Wishlist.jsx` defensively only renders entries where `course` is already a populated object, so that placeholder is never shown as a broken row even if the page happens to be open mid-mutation.

### Models worth knowing

| Model | Purpose |
|---|---|
| `User.js` | Students, teachers, parents (role field) |
| `AdminUser.js` | Separate admin accounts with RBAC + TOTP |
| `Course.js` | Nested structure: modules → lessons |
| `CourseProgress.js` | Per-student lesson completion state |
| `HifzProgress.js` | Quran memorization tracking (surah/verse level) |
| `QuranBookmark.js` | Per-verse Quran bookmarks (one row per user+verse); also carries an optional `note` and highlight `color` |
| `QuranReadingProgress.js` | Reading resume position, daily goal, streak/history (one row per user) |
| `QuranMemorizationStats.js` | Recording-studio practice goal/stats/streak (one row per user, separate from `HifzProgress`) |
| `RefreshToken.js` | JWT refresh family rotation (detects reuse) |
| `SystemConfig.js` | Key-value runtime configuration |
| `SystemAuditLog.js` | Immutable admin action log |

### Deployment

- **Vercel** (live, frontend only): serves the pre-built `frontend/dist` SPA and rewrites `/api/*` to the Render backend URL (see `vercel.json`). No `crons` key is configured here — Vercel does not run any scheduled job, because it does not host the API.
- **Render** (live, backend): `render.yaml` runs `backend/` as a standalone `node server.js` process — not a serverless function. Health check at `/health`.
- **Renewal-reminder cron** (`GET /api/cron/renewal-reminders`, `CRON_SECRET` auth): must be triggered by a scheduler **external to this repo** (e.g. UptimeRobot, GitHub Actions cron) hitting the Render URL daily — `render.yaml` documents this requirement inline, but no such scheduler is configured anywhere in this repository. Confirm one exists before relying on renewal reminders actually sending.
- **Netlify** (`netlify.toml`, present but not live): an alternate frontend-hosting config that exists in the repo but isn't referenced by the live CSP/redirect config — status as an active deployment target is unconfirmed.

No Docker. Deployments themselves are platform-triggered (Vercel/Render), but `.github/workflows/ci.yml` runs backend + frontend tests, lint, and build on push/PR (two parallel jobs; does not deploy anything).

## Key environment variables

Backend (see `backend/.env.example`):
- `MONGO_URI`, `JWT_SECRET`, `JWT_EXPIRES_IN`
- `ADMIN_JWT_ACCESS_SECRET` (64-byte hex), `ADMIN_ENCRYPTION_KEY` (64-char hex, for TOTP secrets)
- `CLIENT_URL` — must match the frontend origin for CORS
- `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`
- `PAYPAL_CLIENT_ID`, `PAYPAL_CLIENT_SECRET`, `PAYPAL_WEBHOOK_ID`
- `SMTP_HOST/PORT/USER/PASS/ADMIN_EMAIL`
- `ADMIN_IP_WHITELIST` — comma-separated IPs/CIDR allowed to hit admin routes. If unset: allows all IPs in development/test, but **fails closed (denies all admin requests)** when `NODE_ENV=production` (`backend/middleware/ipWhitelist.js`) — confirm this var is actually set on Render before deploying, or admin access will be locked out
- `CRON_SECRET` — authenticates the external scheduler's call to `/api/cron/*` (there is no Vercel cron — see Deployment above)
- `REDIS_URL` — optional; enables distributed rate limiting

Frontend: only `VITE_API_URL=/api` (relative; proxied by Vite in dev).

## Git workflow

- Never commit directly to `main`. Every change goes on a feature branch, gets a descriptive commit message (what changed and why — not placeholders like "fix", "update", "kk"), and opens a PR. Use the `/push` command for this.
- Before committing, review `git diff` — don't commit changes you haven't looked at.

## Brand & content conventions

- Brand name: **Al-Rahma** (not "Al Rahma" or "Alrahma")
- Teaching role: **tutor** in English UI strings
- German: **Koran** (not "Quran")
- Prophet honorific: **ﷺ** (Unicode character, not typed-out text)
update @CLAUDE.md file after each major change and keep it up to date
